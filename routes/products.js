const express = require('express');
const router = express.Router();
const Product = require('../models/Product');

// Define product routes (e.g., product listing, creation, etc.)

module.exports = router;
